# Optional: root URL of a self-hosted freej2me-web `web/` directory (trailing slash OK).
# Build from https://github.com/zb3/freej2me-web (Docker + ant) per upstream README.
# VITE_FREEJ2ME_URL=http://127.0.0.1:8080/
